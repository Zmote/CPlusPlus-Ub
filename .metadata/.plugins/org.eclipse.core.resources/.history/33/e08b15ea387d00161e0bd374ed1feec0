#include <wx/wx.h>

class my_first_app : public wxApp
{
public :
	bool OnInit()
	{
		wxFrame* window = new wxFrame(NULL, -1, "hi there");
		MyFrame1 * mywindow = new MyFrame1(NULL,-1,"My Frame");
		window->Show();
		return true;
	}
};

IMPLEMENT_APP(my_first_app);
