#include <iostream>

int main(){
	int anAnswer{};
	while(std::cin){
		if(!(std::cin >> anAnswer)){
			if(std::cin.eof()){
							std::cout << "EOF called";
						}
			if(std::cin.fail()){
				std::cout << "I failed";
			}
		}else{
			std::cout << anAnswer;;
		}
	}
}
