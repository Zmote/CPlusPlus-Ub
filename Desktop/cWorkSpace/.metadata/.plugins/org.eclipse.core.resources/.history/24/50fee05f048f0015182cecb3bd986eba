#include "prime.h"
#include <vector>
#include <algorithm>
#include <functional>
#include <iterator>
#include <numeric>

bool isPrime(const int number){
std::vector<int> range(number);
generate(begin(range),end(range),[](){});
int count = count_if(begin(range),end(range),[&](int i){return number%i == 0;});
return count >=3? false:true;
}
