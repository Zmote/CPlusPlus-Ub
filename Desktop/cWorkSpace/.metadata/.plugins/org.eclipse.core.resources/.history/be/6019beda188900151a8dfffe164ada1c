#include "Ansi.h"
#include <iostream>
using namespace Ansi;

std::string Ansi::Pos(unsigned l, unsigned c) {
	std::string escape = ESCAPE;
    returnESCAPE + std::to_string(l) + ";" + std::to_string(c) + "H";
}
